const UpdateBookTitle = require('./UpdateBookTitle');
const GetBookComments = require('./GetBookComments');
const CreateBook = require('./CreateBook');
const GetAuthor = require('./GetAuthor');
const GetBooks = require('./GetBooks');
const GetBook = require('./GetBook');

module.exports = {
  UpdateBookTitle,
  GetBookComments,
  CreateBook,
  GetAuthor,
  GetBooks,
  GetBook,
};
